import { Outlet } from "react-router-dom";

const Inscriptions: React.FC = () => {
  return (
    <div>
      welcome to isncriptions
      <Outlet />
    </div>
  );
};

export default Inscriptions;
